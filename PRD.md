# 磁力猫（MagnetCat）产品需求文档（PRD）

> **文档版本**: v1.1.0  
> **最后更新**: 2026-05-31  
> **文档状态**: 更新（补充检索接口文档）  
> **产品版本**: v1.0.0

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户故事](#2-用户故事)
3. [功能模块详细说明](#3-功能模块详细说明)
4. [检索接口详细说明](#4-检索接口详细说明)
5. [非功能需求](#5-非功能需求)
6. [数据字典](#6-数据字典)
7. [业务流程图](#7-业务流程图)
8. [项目风险与应对策略](#8-项目风险与应对策略)
9. [附录](#9-附录)

---

## 1. 产品概述

### 1.1 产品背景

在互联网资源获取场景中，磁力链接（Magnet Link）是一种广泛使用的分布式资源定位方式。用户通常需要通过磁力搜索引擎来查找所需的资源链接，但现有搜索方式存在以下痛点：

- **搜索体验差**：网页端搜索广告多、界面杂乱、操作繁琐
- **结果管理难**：搜索到的磁力链接缺乏统一管理，难以收藏和回溯
- **多设备同步缺失**：不同设备间的收藏数据无法同步，切换设备后数据丢失
- **安全性不足**：磁力链接数据明文存储，隐私无法保障

### 1.2 产品定位

**磁力猫（MagnetCat）** 是一款基于 Electron 的桌面端磁力链接搜索与管理工具，定位为轻量级、高隐私、跨平台的本地化搜索客户端。产品以"搜索-收藏-同步"为核心闭环，提供简洁高效的磁力链接获取与管理体验。

### 1.3 目标用户群体

| 用户群体 | 特征描述 | 核心诉求 |
|---------|---------|---------|
| 资源搜索用户 | 频繁搜索各类资源的普通用户 | 快速搜索、一键复制、操作简便 |
| 资源收藏用户 | 需要长期保存和管理磁力链接的用户 | 收藏管理、分类整理、快速回溯 |
| 多设备用户 | 在多台电脑间切换使用的用户 | 数据云同步、跨设备一致性 |
| 隐私敏感用户 | 对数据隐私有较高要求的用户 | 加密存储、本地优先、安全可靠 |

### 1.4 产品目标

| 目标层级 | 描述 | 衡量指标 |
|---------|------|---------|
| 核心目标 | 提供简洁高效的磁力链接搜索体验 | 搜索响应时间 < 5s，搜索成功率 > 90% |
| 重要目标 | 实现磁力链接的本地收藏与云端同步 | 收藏操作响应 < 200ms，同步成功率 > 95% |
| 长期目标 | 构建安全、可扩展的磁力链接管理生态 | 用户留存率 > 60%，NPS > 40 |

### 1.5 产品边界

**当前版本（v1.0.0）包含**：
- 磁力链接关键词搜索
- 搜索结果展示与一键复制
- 本地收藏管理
- 基于 Gitee 的加密云同步
- 深色/浅色主题切换

**当前版本不包含**：
- 用户注册/登录系统
- 磁力链接下载功能
- 资源预览/在线播放
- 多搜索引擎聚合
- 搜索历史记录
- 收藏分类/标签系统

---

## 2. 用户故事

### 2.1 搜索相关

| 编号 | 用户故事 | 验收标准 | 优先级 |
|------|---------|---------|--------|
| US-001 | 作为用户，我想通过关键词搜索磁力链接，以便快速找到所需资源 | 输入关键词后点击搜索或按回车，3秒内开始返回结果 | P0 |
| US-002 | 作为用户，我想看到搜索结果的文件大小，以便判断资源质量 | 每条结果卡片显示文件大小（GB/MB/KB） | P0 |
| US-003 | 作为用户，我想看到搜索结果的文件类型，以便快速筛选 | 每条结果卡片显示文件类型标签（视频/压缩/文档/音频/图片/其他） | P1 |
| US-004 | 作为用户，我想一键复制磁力链接，以便在下载工具中使用 | 点击"复制磁链"按钮后，磁力链接复制到系统剪贴板，按钮显示"已复制"反馈 | P0 |
| US-005 | 作为用户，我想看到搜索进度，以便了解搜索状态 | 搜索过程中显示进度条和已获取数量 | P1 |
| US-006 | 作为用户，我想看到搜索结果按磁力数排序，以便优先查看热门资源 | 结果按磁力数降序排列，磁力数相同时按文件大小降序 | P1 |

### 2.2 收藏相关

| 编号 | 用户故事 | 验收标准 | 优先级 |
|------|---------|---------|--------|
| US-007 | 作为用户，我想收藏感兴趣的磁力链接，以便后续快速访问 | 点击收藏按钮后，链接保存到收藏列表，按钮变为已收藏状态 | P0 |
| US-008 | 作为用户，我想取消收藏，以便移除不再需要的链接 | 在搜索页或收藏页点击已收藏按钮，链接从收藏列表移除 | P0 |
| US-009 | 作为用户，我想查看所有收藏的磁力链接，以便统一管理 | 导航到收藏页可查看所有已收藏链接的列表 | P0 |
| US-010 | 作为用户，我想在收藏页复制磁力链接，以便快速使用 | 收藏页的每条记录都提供"复制磁链"按钮 | P0 |
| US-011 | 作为用户，我想看到收藏数量，以便了解收藏总量 | 工具栏收藏按钮旁显示收藏数量徽标 | P2 |

### 2.3 云同步相关

| 编号 | 用户故事 | 验收标准 | 优先级 |
|------|---------|---------|--------|
| US-012 | 作为用户，我想将收藏数据同步到云端，以便在多设备间共享 | 配置 Gitee 令牌和仓库后，收藏数据加密上传至 Gitee 私有仓库 | P1 |
| US-013 | 作为用户，我想从云端拉取收藏数据，以便在新设备上恢复 | 应用启动时自动从云端拉取最新数据覆盖本地 | P1 |
| US-014 | 作为用户，我想用密码加密同步数据，以便保护隐私 | 数据使用 AES-256-GCM 加密，密码使用 PBKDF2 派生密钥 | P1 |
| US-015 | 作为用户，我想测试云同步连接，以便确认配置正确 | 设置页提供"测试连接"按钮，返回连接成功/失败信息 | P1 |
| US-016 | 作为用户，我想在收藏变更后自动同步，以便保持云端数据最新 | 收藏/取消收藏后自动触发云端同步 | P2 |
| US-017 | 作为用户，我想重置密码，以便在忘记密码时重新配置 | 提供"忘记密码"功能，重置后可用本地数据覆盖云端 | P2 |

### 2.4 界面与体验相关

| 编号 | 用户故事 | 验收标准 | 优先级 |
|------|---------|---------|--------|
| US-018 | 作为用户，我想切换深色/浅色主题，以便适应不同使用环境 | 点击主题切换按钮可在两种主题间平滑切换 | P2 |
| US-019 | 作为用户，我想看到链接预览，以便确认链接正确性 | 每条结果卡片显示磁力链接前70字符的预览 | P2 |
| US-020 | 作为用户，我想在搜索中收藏结果，以便无需切换页面 | 搜索结果卡片右上角提供收藏按钮 | P0 |

---

## 3. 功能模块详细说明

### 3.1 搜索模块

#### 3.1.1 关键词搜索

| 属性 | 描述 |
|------|------|
| **功能点** | 关键词搜索 |
| **优先级** | P0 |
| **描述** | 用户输入关键词，系统向磁力搜索源发起请求，获取匹配的磁力链接列表 |
| **输入** | 搜索关键词（文本字符串） |
| **处理逻辑** | 1. 对关键词进行 Base64 编码；2. 构造搜索 URL 请求搜索页面；3. 解析搜索结果页面，提取标题、链接、文件大小；4. 逐条请求详情页获取磁力链接；5. 计算磁力数（基于哈希的伪随机值）；6. 按磁力数和文件大小排序后逐条推送至渲染进程 |
| **输出** | 磁力链接结果列表（标题、大小、磁力链接、磁力数） |
| **验收标准** | 1. 输入非空关键词后可发起搜索；2. 搜索中按钮显示"搜索中"且不可重复点击；3. 搜索结果逐条流入并按排序规则插入；4. 搜索完成后显示成功/失败统计；5. 搜索失败时显示错误提示 |

#### 3.1.2 搜索结果展示

| 属性 | 描述 |
|------|------|
| **功能点** | 搜索结果展示 |
| **优先级** | P0 |
| **描述** | 以卡片网格形式展示搜索结果，每张卡片包含资源标题、文件大小、文件类型、磁力数、链接预览和操作按钮 |
| **排序规则** | 主排序：磁力数降序；次排序：文件大小降序 |
| **验收标准** | 1. 结果以响应式网格布局展示（最小列宽 280px）；2. 每张卡片显示序号、标题、文件大小、文件类型、磁力数；3. 卡片入场有滑入动画；4. 标题过长时单行省略显示 |

#### 3.1.3 一键复制磁力链接

| 属性 | 描述 |
|------|------|
| **功能点** | 一键复制磁力链接 |
| **优先级** | P0 |
| **描述** | 点击"复制磁链"按钮，将完整磁力链接复制到系统剪贴板 |
| **验收标准** | 1. 点击按钮后磁力链接写入系统剪贴板；2. 按钮文字变为"已复制"并变为绿色；3. 2.5秒后按钮恢复原状；4. 复制失败时无崩溃 |

#### 3.1.4 文件类型识别

| 属性 | 描述 |
|------|------|
| **功能点** | 文件类型识别 |
| **优先级** | P1 |
| **描述** | 根据资源标题中的文件扩展名自动识别文件类型 |
| **识别规则** | 视频文件：.mp4/.mkv/.avi/.mov/.wmv/.flv；压缩文件：.rar/.zip/.7z/.tar/.gz；文档文件：.pdf/.doc/.docx/.txt/.xls/.xlsx；音频文件：.mp3/.wav/.flac/.aac/.ogg；图片文件：.jpg/.jpeg/.png/.gif/.bmp/.svg；其他：无匹配扩展名 |
| **验收标准** | 1. 每条结果卡片显示文件类型标签；2. 不同类型使用不同颜色标签；3. 无法识别时显示"其他文件" |

### 3.2 收藏模块

#### 3.2.1 添加收藏

| 属性 | 描述 |
|------|------|
| **功能点** | 添加收藏 |
| **优先级** | P0 |
| **描述** | 在搜索结果卡片上点击收藏按钮，将磁力链接添加到本地收藏列表 |
| **存储方式** | localStorage，键名 `magnet_favorites` |
| **验收标准** | 1. 点击空心收藏按钮后变为实心红色；2. 收藏数据即时写入 localStorage；3. 工具栏收藏计数实时更新；4. 重复收藏同一链接不产生重复数据；5. 收藏变更后触发云同步（如已配置） |

#### 3.2.2 取消收藏

| 属性 | 描述 |
|------|------|
| **功能点** | 取消收藏 |
| **优先级** | P0 |
| **描述** | 在搜索页或收藏页点击已收藏按钮，将磁力链接从收藏列表移除 |
| **验收标准** | 1. 点击实心收藏按钮后变为空心；2. 收藏页中移除后卡片消失；3. 数据即时从 localStorage 中删除；4. 工具栏收藏计数实时更新 |

#### 3.2.3 收藏列表查看

| 属性 | 描述 |
|------|------|
| **功能点** | 收藏列表查看 |
| **优先级** | P0 |
| **描述** | 在收藏页查看所有已收藏的磁力链接，以卡片网格形式展示 |
| **验收标准** | 1. 收藏页显示所有已收藏链接；2. 无收藏时显示空状态引导页；3. 每条收藏支持复制和取消收藏操作；4. 收藏页提供"返回搜索"导航按钮 |

### 3.3 云同步模块

#### 3.3.1 Gitee 配置管理

| 属性 | 描述 |
|------|------|
| **功能点** | Gitee 配置管理 |
| **优先级** | P1 |
| **描述** | 配置 Gitee 私人令牌和仓库名称，用于云端数据同步 |
| **配置项** | 私人令牌（Token）、仓库名称（owner/repo 格式）、加密密码 |
| **验收标准** | 1. 令牌输入框为密码类型，隐藏显示；2. 仓库名称验证 owner/repo 格式；3. 密码最少6位，显示密码强度指示器；4. 已配置时令牌和密码显示"已保存"状态；5. 提供"测试连接"按钮验证配置有效性 |

#### 3.3.2 数据加密

| 属性 | 描述 |
|------|------|
| **功能点** | 数据加密 |
| **优先级** | P1 |
| **描述** | 使用 AES-256-GCM 算法对云端同步数据和本地令牌进行加密 |
| **加密方案** | 密钥派生：PBKDF2-SHA256（100,000次迭代，32字节密钥）；加密算法：AES-256-GCM（12字节IV，16字节 Salt，16字节 AuthTag）；令牌加密：使用用户密码派生密钥加密；密码本地存储：使用机器特征密钥加密（AES-256-GCM）；密码验证：PBKDF2-SHA256 哈希比对 |
| **验收标准** | 1. 云端数据文件为加密后的 Base64 字符串；2. 不同密码生成不同的同步文件路径；3. 令牌不以明文存储在本地；4. 密码不以明文存储在本地 |

#### 3.3.3 推送同步（Push）

| 属性 | 描述 |
|------|------|
| **功能点** | 推送同步 |
| **优先级** | P1 |
| **描述** | 将本地收藏数据加密后上传至 Gitee 仓库 |
| **处理逻辑** | 1. 读取本地收藏数据；2. 构建同步数据结构（版本号、时间戳、数据列表）；3. 使用用户密码加密数据；4. 计算同步文件路径（基于密码哈希）；5. 检查云端是否已有该文件；6. 存在则更新，不存在则创建 |
| **验收标准** | 1. 推送成功后更新 lastSyncTime；2. 推送失败时显示错误信息；3. 支持最多3次重试，重试间隔2秒 |

#### 3.3.4 拉取同步（Pull）

| 属性 | 描述 |
|------|------|
| **功能点** | 拉取同步 |
| **优先级** | P1 |
| **描述** | 从 Gitee 仓库拉取加密数据，解密后覆盖本地收藏 |
| **处理逻辑** | 1. 解密本地存储的密码和令牌；2. 获取云端加密文件；3. 解密文件内容；4. 解析 JSON 数据；5. 验证数据有效性；6. 覆盖本地 localStorage |
| **验收标准** | 1. 拉取成功后本地收藏更新为云端数据；2. 云端无数据时返回提示而非错误；3. 解密失败时提示密码可能不正确；4. 拉取成功后更新 lastSyncTime |

#### 3.3.5 自动同步

| 属性 | 描述 |
|------|------|
| **功能点** | 自动同步 |
| **优先级** | P2 |
| **描述** | 应用启动时自动拉取云端数据，收藏变更后自动推送 |
| **触发时机** | 1. 应用启动3秒后自动拉取；2. 收藏/取消收藏后延迟推送（防抖） |
| **验收标准** | 1. 启动时自动拉取不阻塞界面；2. 拉取成功后收藏列表和计数更新；3. 收藏变更后3秒内触发推送；4. 未配置云同步时不触发自动同步 |

#### 3.3.6 密码管理

| 属性 | 描述 |
|------|------|
| **功能点** | 密码管理 |
| **优先级** | P2 |
| **描述** | 提供密码解锁、自动解锁和密码重置功能 |
| **子功能** | 1. 密码解锁：输入密码验证后解锁配置；2. 自动解锁：使用机器特征密钥解密本地密码自动解锁；3. 密码重置：清除本地加密数据，重新配置（本地数据覆盖云端） |
| **验收标准** | 1. 已有配置时启动显示解锁界面；2. 自动解锁成功时无需手动输入密码；3. 密码错误时显示"密码验证失败"；4. 重置密码需二次确认；5. 重置后保存时使用本地数据覆盖云端 |

### 3.4 界面与交互模块

#### 3.4.1 主题切换

| 属性 | 描述 |
|------|------|
| **功能点** | 主题切换 |
| **优先级** | P2 |
| **描述** | 支持浅色/深色两种主题，切换时全局平滑过渡 |
| **存储方式** | localStorage，键名 `darkMode` |
| **验收标准** | 1. 点击主题切换按钮可切换主题；2. 切换时所有元素0.25s平滑过渡；3. 主题选择持久化保存；4. 页面加载前应用已保存的主题（防止闪烁） |

#### 3.4.2 自定义标题栏

| 属性 | 描述 |
|------|------|
| **功能点** | 自定义标题栏 |
| **优先级** | P2 |
| **描述** | 使用无边框窗口 + 自定义标题栏，提供最小化、最大化/还原、关闭按钮 |
| **验收标准** | 1. 标题栏高度32px，可拖拽移动窗口；2. 最小化/最大化/关闭按钮功能正常；3. 关闭按钮悬停时变红色；4. 标题栏显示应用图标和名称 |

#### 3.4.3 状态栏

| 属性 | 描述 |
|------|------|
| **功能点** | 状态栏 |
| **优先级** | P2 |
| **描述** | 底部固定状态栏，显示当前操作状态和结果计数 |
| **验收标准** | 1. 状态栏固定在窗口底部；2. 左侧显示状态指示灯和状态文字；3. 右侧显示结果计数；4. 状态文字随操作实时更新（就绪/正在搜索/搜索完成/搜索失败） |

---

## 4. 检索接口详细说明

> 本章档详细描述磁力猫搜索功能所依赖的外部检索接口及其返回数据的处理规范。所有接口均为第三方磁力搜索源提供的 HTTP 接口，本应用作为客户端进行调用和数据解析。

### 4.1 接口概览

磁力猫的搜索功能涉及两个核心外部接口：

| 接口 | 用途 | 调用时机 |
|------|------|---------|
| 搜索列表接口 | 根据关键词获取搜索结果列表（含标题、详情页链接、文件大小） | 用户发起搜索时调用 |
| 详情页接口 | 根据详情页链接获取磁力链接 | 对每条搜索结果逐一调用 |

### 4.2 搜索列表接口

#### 4.2.1 接口基本信息

| 属性 | 值 |
|------|-----|
| **接口名称** | 磁力搜索列表查询 |
| **HTTP 方法** | `GET` |
| **接口 URL** | `https://clm50.top/search` |
| **协议** | HTTPS |
| **响应格式** | HTML（text/html） |
| **字符编码** | UTF-8 |
| **超时时间** | 30,000ms（30秒） |

#### 4.2.2 请求参数

| 参数名 | 数据类型 | 是否必填 | 取值范围/格式 | 描述 |
|--------|---------|---------|-------------|------|
| `word` | String | 是 | 非 Base64 编码后的任意 UTF-8 字符串 | 搜索关键词，需经 Base64 编码后传入，编码时去除 `=` 填充符 |

**参数编码规则**：

1. 原始关键词为用户输入的 UTF-8 文本
2. 使用 `Buffer.from(keyword, 'utf-8').toString('base64')` 进行标准 Base64 编码
3. 移除编码结果末尾的所有 `=` 填充符
4. 编码后的字符串直接作为 `word` 参数值

**编码示例**：

| 原始关键词 | Base64 编码结果（去除填充符） | 完整请求 URL |
|-----------|--------------------------|-------------|
| `电影` | `5LuK5a2Q` | `https://clm50.top/search?word=5LuK5a2Q` |
| `test` | `dGVzdA` | `https://clm50.top/search?word=dGVzdA` |

#### 4.2.3 请求头（Request Headers）

| Header 名称 | 值 | 是否必填 | 描述 |
|-------------|-----|---------|------|
| `User-Agent` | `Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36` | 是 | 模拟浏览器访问，防止被服务器拒绝 |
| `Accept` | `text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8` | 是 | 声明接受的响应内容类型 |
| `Accept-Language` | `zh-CN,zh;q=0.9,en;q=0.8` | 是 | 声明优先中文内容 |
| `Referer` | `https://clm50.top/` | 是 | 标识来源页面，防止防盗链拒绝 |

> **注意**：请求头中必须包含 `User-Agent` 和 `Referer`，否则可能被服务器识别为爬虫请求并拒绝访问。

#### 4.2.4 响应数据结构

响应为 HTML 页面，非结构化 JSON。需要通过正则表达式从 HTML 中提取数据。

**HTML 页面关键结构**：

```html
<!-- 搜索结果条目 -->
<div class="Search_title_wrapper" ...>
  <a href="/information/XXXXXX">资源标题</a>
</div>

<!-- 文件大小信息 -->
<em>文件大小：</em>2.5 GB
```

#### 4.2.5 数据提取规则

##### 4.2.5.1 链接与标题提取

| 属性 | 值 |
|------|-----|
| **正则表达式** | `/<div class="Search_title_wrapper"[^>]*>.*?<a[^>]+href="(\/information\/[^"]+)"[^>]*>(.*?)<\/a>/gs` |
| **匹配模式** | 全局匹配（`g` 标志）+ 单行模式（`s` 标志，使 `.` 匹配换行符） |
| **匹配组1** | 详情页相对路径，格式为 `/information/{resourceId}` |
| **匹配组2** | 资源标题（可能包含 HTML 标签，需后续清理） |

**提取后处理**：

1. 对匹配组2（标题）执行 `replace(/<[^>]+>/g, '').trim()`，移除所有 HTML 标签并去除首尾空白
2. 匹配组1（链接）为相对路径，需拼接基础 URL `https://clm50.top` 构造完整详情页 URL

**提取结果数据结构**：

```typescript
interface SearchResultLink {
  link: string;   // 详情页相对路径，如 "/information/abc123"
  title: string;  // 清理后的资源标题，如 "示例资源名称.mp4"
}
```

##### 4.2.5.2 文件大小提取

| 属性 | 值 |
|------|-----|
| **正则表达式** | `/<em>文件大小：<\/em>([\d.]+)\s*([GMK]B)/g` |
| **匹配模式** | 全局匹配（`g` 标志） |
| **匹配组1** | 文件大小数值（浮点数），如 `2.5`、`1024` |
| **匹配组2** | 文件大小单位，取值为 `GB`、`MB` 或 `KB` |

**提取后处理**：

1. 匹配组1使用 `parseFloat()` 转换为数字
2. 根据匹配组2的单位进行标准化换算：
   - `GB` → `value * 1024 * 1024`（换算为 KB 当量）
   - `MB` → `value * 1024`（换算为 KB 当量）
   - `KB` → `value`（保持原值）

**提取结果数据结构**：

```typescript
interface SearchResultSize {
  value: number;  // 文件大小数值，如 2.5
  unit: string;   // 文件大小单位，"GB" | "MB" | "KB"
}
```

##### 4.2.5.3 链接与大小数据合并

链接列表和大小列表按索引一一对应合并，取两者长度的较小值作为有效结果数：

```typescript
interface SearchResultItem {
  link: string;      // 详情页相对路径
  title: string;     // 清理后的资源标题
  sizeText: string;  // 文件大小文本，如 "2.5 GB"
  sizeBytes: number; // 文件大小标准化值（KB当量）
}
```

**合并逻辑**：

1. 遍历 `i = 0` 到 `Math.min(links.length, sizes.length) - 1`
2. 对每个 `i`，合并 `links[i]` 和 `sizes[i]`
3. 标题执行 HTML 标签清理
4. 文件大小根据单位换算为标准化值
5. `sizeText` 格式为 `{value} {unit}`（如 `"2.5 GB"`）

#### 4.2.6 响应状态码与错误处理

| HTTP 状态码 | 含义 | 处理方式 |
|------------|------|---------|
| `200` | 请求成功 | 解析 HTML 内容，提取搜索结果 |
| `301/302` | 重定向 | axios 自动跟随重定向（默认行为） |
| `403` | 禁止访问 | 可能因请求头缺失或被识别为爬虫，发送 `search-error` 事件 |
| `404` | 页面不存在 | 发送 `search-error` 事件，提示搜索失败 |
| `429` | 请求过于频繁 | 发送 `search-error` 事件，提示稍后重试 |
| `500/502/503` | 服务器错误 | 发送 `search-error` 事件，提示服务器异常 |
| `ECONNABORTED` | 请求超时 | 发送 `search-error` 事件，提示网络超时 |
| `ENOTFOUND` | DNS 解析失败 | 发送 `search-error` 事件，提示网络连接失败 |
| `ECONNREFUSED` | 连接被拒绝 | 发送 `search-error` 事件，提示服务器不可达 |

**空结果处理**：

| 场景 | 条件 | 处理方式 |
|------|------|---------|
| 无链接结果 | `links.length === 0` | 发送 `search-error` 事件，消息为"未找到任何搜索结果" |
| 无大小数据 | `sizes.length === 0` | 发送 `search-error` 事件，消息为"未找到任何搜索结果" |
| 链接与大小数量不匹配 | `links.length !== sizes.length` | 取较小值，按索引对齐合并，不报错 |

**错误事件格式**：

```typescript
// search-error 事件数据
eventType: 'search-error'
data: string  // 错误描述信息，如 "未找到任何搜索结果" 或 axios 错误的 message 属性
```

---

### 4.3 详情页接口（磁力链接获取）

#### 4.3.1 接口基本信息

| 属性 | 值 |
|------|-----|
| **接口名称** | 磁力链接详情获取 |
| **HTTP 方法** | `GET` |
| **接口 URL** | `https://clm50.top/information/{resourceId}` |
| **协议** | HTTPS |
| **响应格式** | HTML（text/html） |
| **字符编码** | UTF-8 |
| **超时时间** | 15,000ms（15秒） |

#### 4.3.2 请求参数

| 参数名 | 数据类型 | 是否必填 | 取值范围/格式 | 描述 |
|--------|---------|---------|-------------|------|
| `{resourceId}` | String（路径参数） | 是 | 搜索列表接口返回的链接路径 | 资源唯一标识，从搜索列表结果的 `link` 字段获取，拼接到 URL 路径中 |

**URL 构造规则**：

- 基础 URL：`https://clm50.top`
- 搜索列表返回的 `link` 字段为相对路径，如 `/information/abc123`
- 完整 URL = 基础 URL + link，如 `https://clm50.top/information/abc123`

#### 4.3.3 请求头（Request Headers）

与搜索列表接口完全一致，参见 [4.2.3 请求头](#423-请求头request-headers)。

#### 4.3.4 响应数据结构

响应为 HTML 页面，需从中提取磁力链接。

**HTML 页面关键结构**：

```html
<input id="Information_copy_text" value="magnet:?xt=urn:btih:XXXXXX..." />
```

#### 4.3.5 数据提取规则

##### 4.3.5.1 磁力链接提取

| 属性 | 值 |
|------|-----|
| **正则表达式** | `/<input id="Information_copy_text" value="(magnet:\?xt=urn:btih:[^"]+)"/` |
| **匹配模式** | 非全局匹配（仅取首个匹配） |
| **匹配组1** | 完整磁力链接，格式为 `magnet:?xt=urn:btih:{hash}...` |

**提取结果数据结构**：

```typescript
interface MagnetDetail {
  magnet: string;      // 完整磁力链接
  magnetCount: number; // 磁力数（基于哈希计算，1-50）
}
```

##### 4.3.5.2 磁力数计算

磁力数是基于磁力链接哈希值计算的伪随机数，用于模拟资源热度排序。

**计算算法**：

```
输入: magnetHash (String) - 完整磁力链接
输出: magnetCount (Number) - 磁力数，范围 1-50

步骤:
1. 初始化 hash = 0
2. 遍历 magnetHash 的每个字符:
   a. charCode = 当前字符的 Unicode 编码
   b. hash = ((hash << 5) - hash) + charCode
   c. hash = hash & hash  (转为 32 位有符号整数)
3. magnetCount = Math.abs(hash % 50) + 1
4. 返回 magnetCount
```

**特性说明**：
- 同一磁力链接始终产生相同的磁力数
- 磁力数范围固定为 1-50
- 算法为确定性算法，非真随机

#### 4.3.6 响应状态码与错误处理

| HTTP 状态码 | 含义 | 处理方式 |
|------------|------|---------|
| `200` | 请求成功 | 解析 HTML，提取磁力链接 |
| `404` | 详情页不存在 | 计入 `failCount`，跳过该条结果 |
| `403` | 禁止访问 | 计入 `failCount`，跳过该条结果 |
| `500/502/503` | 服务器错误 | 计入 `failCount`，跳过该条结果 |
| 网络异常 | 连接/超时错误 | 计入 `failCount`，跳过该条结果 |
| 无磁力链接 | 正则不匹配 | 计入 `failCount`，跳过该条结果 |

> **重要**：详情页接口的错误不会中断整体搜索流程，仅计入失败计数并继续处理下一条结果。

---

### 4.4 搜索结果完整数据流

#### 4.4.1 最终输出数据结构

经过搜索列表接口和详情页接口的完整处理后，每条搜索结果输出以下数据：

```typescript
interface MagnetSearchResult {
  title: string;       // 资源标题（已清理 HTML 标签）
  size: string;        // 文件大小文本，如 "2.5 GB"
  sizeBytes: number;   // 文件大小标准化值（KB 当量，用于排序）
  magnet: string;      // 完整磁力链接
  magnetCount: number; // 磁力数（1-50，用于排序）
}
```

#### 4.4.2 数据字段详细说明

| 字段名 | 数据类型 | 来源 | 生成规则 | 用途 |
|--------|---------|------|---------|------|
| `title` | String | 搜索列表接口 | 从 HTML 提取后清理标签 | 展示资源名称 |
| `size` | String | 搜索列表接口 | 格式化输出 `{value} {unit}` | 展示文件大小 |
| `sizeBytes` | Number | 搜索列表接口 | GB→`×1048576`，MB→`×1024`，KB→原值 | 排序比较 |
| `magnet` | String | 详情页接口 | 从 HTML 提取 | 磁力链接复制/收藏 |
| `magnetCount` | Number | 本地计算 | 基于磁力链接哈希算法 | 排序比较 |

#### 4.4.3 排序规则

搜索结果在渲染进程中按以下规则排序插入：

1. **主排序**：`magnetCount` 降序（磁力数大的排前面）
2. **次排序**：`sizeBytes` 降序（文件大的排前面）

> 排序在 DOM 插入时实时执行（`insertCardInOrder` 方法），而非搜索完成后统一排序。

---

### 4.5 IPC 事件序列

搜索过程中，主进程通过以下 IPC 事件序列向渲染进程推送数据：

```
时间线 ──────────────────────────────────────────────────→

主进程事件:
  ① search-total(totalCount)
       │
       │  ② search-result(item1)  ── 每条结果获取成功后立即发送
       │  ② search-result(item2)
       │  ② search-result(item3)
       │      ...
       │
  ③ search-complete({success, fail, duration})
```

| 序号 | 事件名 | 方向 | 触发时机 | 数据类型 | 描述 |
|------|--------|------|---------|---------|------|
| ① | `search-total` | 主→渲染 | 搜索列表解析完成 | `Number` | 搜索结果总数，用于显示进度 |
| ② | `search-result` | 主→渲染 | 每条详情页解析成功 | `MagnetSearchResult` | 单条搜索结果，渲染进程接收后按排序规则插入 DOM |
| ③ | `search-complete` | 主→渲染 | 所有详情页处理完毕 | `Object` | 搜索完成统计 |
| - | `search-error` | 主→渲染 | 搜索列表请求失败或无结果 | `String` | 错误描述信息 |

**`search-complete` 事件数据结构**：

```typescript
interface SearchCompleteStats {
  success: number;  // 成功获取磁力链接的数量
  fail: number;     // 获取失败的数量（含网络错误和正则不匹配）
  duration: number; // 搜索总耗时（毫秒）
}
```

---

### 4.6 请求频率控制

| 控制项 | 值 | 描述 |
|--------|-----|------|
| 搜索列表请求 | 1次/搜索 | 每次搜索仅发起1次列表请求 |
| 详情页请求间隔 | 100ms | 每两条详情页请求之间间隔100毫秒 |
| 详情页请求超时 | 15,000ms | 单条详情页请求超时时间 |
| 搜索列表请求超时 | 30,000ms | 搜索列表请求超时时间 |
| 并发控制 | 串行 | 详情页请求为串行执行，不并发 |

> **最佳实践建议**：100ms 的请求间隔是为了避免对搜索源服务器造成过大压力，同时降低被限流或封禁的风险。如需提升搜索速度，可考虑适当缩短间隔但不建议低于 50ms。

---

### 4.7 数据处理标准流程

#### 4.7.1 搜索列表数据处理流程

```
步骤1: 接收搜索关键词
  │
  ▼
步骤2: 关键词 Base64 编码（去除 = 填充符）
  │
  ▼
步骤3: 构造请求 URL 和请求头
  │
  ▼
步骤4: 发送 GET 请求，等待响应
  │
  ├── 成功(200) → 继续
  ├── 网络错误 → 发送 search-error，终止
  └── HTTP错误 → 发送 search-error，终止
  │
  ▼
步骤5: 使用 linkRegex 正则提取链接列表
  │
  ▼
步骤6: 使用 sizeRegex 正则提取大小列表
  │
  ▼
步骤7: 校验提取结果
  ├── links.length === 0 或 sizes.length === 0 → 发送 search-error，终止
  └── 有效数据 → 继续
  │
  ▼
步骤8: 按索引合并链接与大小数据
  │  · 遍历范围: 0 ~ min(links.length, sizes.length) - 1
  │  · 标题清理: 移除 HTML 标签
  │  · 大小标准化: GB/MB/KB → 统一换算
  │
  ▼
步骤9: 发送 search-total 事件（结果总数）
  │
  ▼
步骤10: 逐条请求详情页获取磁力链接
```

#### 4.7.2 详情页数据处理流程

```
步骤1: 构造详情页 URL (baseUrl + link)
  │
  ▼
步骤2: 发送 GET 请求（相同请求头，超时15s）
  │
  ├── 成功(200) → 继续
  ├── 网络错误 → failCount++，跳过
  └── HTTP错误 → failCount++，跳过
  │
  ▼
步骤3: 使用 magnetRegex 正则提取磁力链接
  │
  ├── 匹配成功 → 继续
  └── 匹配失败 → failCount++，跳过
  │
  ▼
步骤4: 计算磁力数 (generateMagnetCount)
  │
  ▼
步骤5: 构建结果对象 {title, size, sizeBytes, magnet, magnetCount}
  │
  ▼
步骤6: 发送 search-result 事件
  │
  ▼
步骤7: 等待 100ms 间隔
  │
  ▼
步骤8: 处理下一条结果（返回步骤1）
```

#### 4.7.3 搜索完成处理流程

```
步骤1: 所有结果处理完毕
  │
  ▼
步骤2: 统计成功/失败数量和总耗时
  │
  ▼
步骤3: 发送 search-complete 事件
  │  data: { success: number, fail: number, duration: number }
  │
  ▼
步骤4: 渲染进程更新状态栏
  │  · 状态文字: "搜索完成"
  · 结果计数: "共 N 个结果"
```

---

### 4.8 异常场景与容错策略

| 异常场景 | 影响范围 | 容错策略 | 用户感知 |
|---------|---------|---------|---------|
| 搜索源站点不可用 | 全部搜索功能 | 捕获网络异常，发送 search-error 事件 | 显示"搜索失败"状态 |
| 搜索源页面结构变更 | 数据提取失败 | 正则不匹配时 links/sizes 为空，触发"未找到任何搜索结果" | 显示"未找到任何搜索结果" |
| 详情页无磁力链接 | 单条结果 | 计入 failCount，跳过该条，继续处理下一条 | 该条结果不显示，不影响其他结果 |
| 详情页请求超时 | 单条结果 | axios 超时异常捕获，计入 failCount | 该条结果不显示，不影响其他结果 |
| 部分详情页服务器错误 | 单条结果 | HTTP 错误捕获，计入 failCount | 该条结果不显示，不影响其他结果 |
| 链接数与大小数不匹配 | 数据对齐 | 取较小值按索引对齐 | 可能丢失末尾无大小数据的结果 |
| 标题含 HTML 标签 | 数据展示 | 使用 `replace(/<[^>]+>/g, '').trim()` 清理 | 标题显示为纯文本 |
| 磁力链接含特殊字符 | 数据存储 | 磁力链接原样保存，不进行额外编码 | 收藏和复制功能正常 |

---

### 4.9 接口变更风险与适配指南

#### 4.9.1 已知风险

| 风险项 | 描述 | 影响程度 |
|--------|------|---------|
| 域名变更 | 搜索源域名 `clm50.top` 可能变更 | 高（搜索功能完全不可用） |
| 页面结构变更 | HTML class 名称或 DOM 结构可能变更 | 高（正则提取失败，无法获取结果） |
| 反爬策略升级 | 搜索源可能增加验证码、频率限制等 | 中（搜索成功率下降） |
| HTTPS 证书问题 | 证书过期或配置错误 | 中（请求失败） |

#### 4.9.2 适配指南

当搜索源发生变更时，需按以下步骤进行适配：

1. **域名变更**：修改 `main.js` 中 `searchUrl` 和 `detailUrl` 的基础域名
2. **页面结构变更**：
   - 使用浏览器开发者工具分析新的 HTML 结构
   - 更新 `linkRegex` 正则表达式以匹配新的链接元素
   - 更新 `sizeRegex` 正则表达式以匹配新的大小元素
   - 更新 `magnetRegex` 正则表达式以匹配新的磁力链接元素
3. **反爬策略升级**：
   - 更新 `User-Agent` 为最新浏览器版本
   - 增加请求间隔时间
   - 考虑添加 Cookie 管理或验证码处理
4. **建议重构方向**：
   - 将搜索源配置（URL、正则、请求头）抽取为可配置项
   - 支持多搜索源切换
   - 增加搜索源健康检测机制

---

### 4.10 正则表达式参考

本节列出所有用于数据提取的正则表达式及其详细说明，供开发人员参考和维护。

#### 4.10.1 链接提取正则

```
/<div class="Search_title_wrapper"[^>]*>.*?<a[^>]+href="(\/information\/[^"]+)"[^>]*>(.*?)<\/a>/gs
```

| 部分 | 说明 |
|------|------|
| `<div class="Search_title_wrapper"[^>]*>` | 匹配搜索结果标题容器 div，`[^>]*` 容许额外属性 |
| `.*?` | 非贪婪匹配 div 与 a 标签之间的任意内容（含换行，因 `s` 标志） |
| `<a[^>]+href="` | 匹配 a 标签及其 href 属性开始 |
| `(\/information\/[^"]+)` | **捕获组1**：匹配以 `/information/` 开头的相对路径 |
| `"[^>]*>` | 匹配 href 属性结束和 a 标签闭合 |
| `(.*?)` | **捕获组2**：非贪婪匹配 a 标签内的标题文本（可能含 HTML 标签） |
| `<\/a>` | 匹配 a 标签结束 |
| `g` 标志 | 全局匹配，提取所有结果 |
| `s` 标志 | 单行模式，使 `.` 匹配换行符 |

#### 4.10.2 文件大小提取正则

```
/<em>文件大小：<\/em>([\d.]+)\s*([GMK]B)/g
```

| 部分 | 说明 |
|------|------|
| `<em>文件大小：<\/em>` | 匹配文件大小标签前缀（中文冒号） |
| `([\d.]+)` | **捕获组1**：匹配数值部分（含小数点） |
| `\s*` | 匹配数值与单位之间的空白字符 |
| `([GMK]B)` | **捕获组2**：匹配单位，仅支持 GB、MB、KB |
| `g` 标志 | 全局匹配，提取所有文件大小 |

#### 4.10.3 磁力链接提取正则

```
/<input id="Information_copy_text" value="(magnet:\?xt=urn:btih:[^"]+)"/
```

| 部分 | 说明 |
|------|------|
| `<input id="Information_copy_text" value="` | 匹配特定 id 的 input 元素 |
| `(magnet:\?xt=urn:btih:[^"]+)` | **捕获组1**：匹配磁力链接，以 `magnet:?xt=urn:btih:` 开头 |
| `"` | 匹配 value 属性值的结束引号 |
| 无 `g` 标志 | 仅匹配第一个结果（每页仅一个磁力链接） |

> **注意**：正则中 `?` 需要转义为 `\?`，因为 `?` 在正则中是量词字符。

---

## 5. 非功能需求

### 5.1 性能需求

| 编号 | 需求描述 | 指标 |
|------|---------|------|
| NFR-P01 | 搜索请求响应时间 | 首条结果返回时间 < 5秒（网络正常） |
| NFR-P02 | 界面渲染性能 | 卡片列表滚动帧率 ≥ 30fps |
| NFR-P03 | 收藏操作响应时间 | 本地收藏/取消收藏 < 200ms |
| NFR-P04 | 主题切换响应时间 | 主题切换过渡动画 ≤ 300ms |
| NFR-P05 | 内存占用 | 应用运行时内存占用 ≤ 200MB |
| NFR-P06 | 安装包体积 | Windows 安装包 ≤ 80MB |
| NFR-P07 | 搜索并发控制 | 搜索结果逐条获取，每条间隔100ms，避免请求过快 |
| NFR-P08 | 云同步超时 | API 请求超时时间：搜索30s，详情15s，Gitee API 15s |

### 5.2 安全需求

| 编号 | 需求描述 | 优先级 |
|------|---------|--------|
| NFR-S01 | 云端数据加密：使用 AES-256-GCM 加密所有同步数据 | P0 |
| NFR-S02 | 密钥派生安全：使用 PBKDF2-SHA256（100,000次迭代）派生加密密钥 | P0 |
| NFR-S03 | 令牌保护：Gitee 令牌使用用户密码加密后本地存储，不以明文保存 | P0 |
| NFR-S04 | 密码本地保护：密码使用机器特征密钥加密后本地存储 | P0 |
| NFR-S05 | XSS 防护：所有用户输入和外部数据在渲染前进行 HTML 转义 | P0 |
| NFR-S06 | 通信安全：所有外部请求使用 HTTPS 协议 | P1 |
| NFR-S07 | 上下文隔离：当前 `contextIsolation: false`，存在安全风险，后续版本应启用 | P1（改进项） |
| NFR-S08 | nodeIntegration：当前 `nodeIntegration: true`，存在安全风险，后续版本应使用 preload 脚本 | P1（改进项） |

### 5.3 兼容性需求

| 编号 | 需求描述 |
|------|---------|
| NFR-C01 | 操作系统：支持 Windows 10/11（x64），macOS（x64/arm64） |
| NFR-C02 | Electron 版本：基于 Electron 28.x |
| NFR-C03 | 屏幕分辨率：最低支持 1000×700 像素 |
| NFR-C04 | 响应式布局：支持窗口缩放，卡片网格自适应列数 |
| NFR-C05 | 网络环境：需要互联网连接（搜索和云同步功能） |

### 5.4 可靠性需求

| 编号 | 需求描述 |
|------|---------|
| NFR-R01 | 搜索失败时显示错误提示，不崩溃 |
| NFR-R02 | 云同步失败时不影响本地功能正常使用 |
| NFR-R03 | 云同步推送支持最多3次重试 |
| NFR-R04 | localStorage 数据损坏时使用空数组兜底 |
| NFR-R05 | Gitee API 限流时显示友好提示 |

### 5.5 可维护性需求

| 编号 | 需求描述 |
|------|---------|
| NFR-M01 | 调试模式：支持独立调试窗口，实时显示运行日志 |
| NFR-M02 | 日志分级：支持 INFO/WARN/ERROR/DEBUG 四级日志 |
| NFR-M03 | 构建自动化：提供 PowerShell 构建脚本，一键打包发布 |

---

## 6. 数据字典

### 6.1 本地存储数据

#### 6.1.1 收藏数据（localStorage）

| 字段 | 类型 | 必填 | 描述 |
|------|------|------|------|
| `title` | String | 是 | 资源标题 |
| `size` | String | 是 | 文件大小文本（如 "2.5 GB"） |
| `sizeBytes` | Number | 是 | 文件大小字节数（用于排序） |
| `magnet` | String | 是 | 完整磁力链接（magnet:?xt=urn:btih:...），同时作为唯一标识 |
| `magnetCount` | Number | 是 | 磁力数（基于哈希的伪随机值，1-50） |

**存储键**: `magnet_favorites`  
**存储格式**: JSON 字符串，内容为上述对象的数组  
**示例**:
```json
[
  {
    "title": "示例资源名称.mp4",
    "size": "2.5 GB",
    "sizeBytes": 2621440,
    "magnet": "magnet:?xt=urn:btih:abc123def456...",
    "magnetCount": 35
  }
]
```

#### 6.1.2 主题设置（localStorage）

| 字段 | 类型 | 必填 | 描述 |
|------|------|------|------|
| `darkMode` | String | 是 | 深色模式开关，"true" 为深色，其他为浅色 |

#### 6.1.3 云同步配置文件（Electron userData 目录）

**文件名**: `cloud_settings.json`  
**存储路径**: `%APPDATA%/magnet-cat/cloud_settings.json`（Windows）

| 字段 | 类型 | 必填 | 描述 |
|------|------|------|------|
| `repo` | String | 是 | Gitee 仓库名称（owner/repo 格式） |
| `lastSyncTime` | String | 否 | 上次同步时间（ISO 8601 格式） |
| `pwd_hash` | String | 条件必填 | 密码验证哈希（PBKDF2-SHA256，Base64 编码），设置密码后必填 |
| `enc_token` | String | 条件必填 | AES-256-GCM 加密后的 Gitee 令牌，设置密码后必填 |
| `enc_pwd` | String | 条件必填 | 机器密钥加密后的用户密码，设置密码后必填 |

### 6.2 云端同步数据

#### 6.2.1 同步文件数据结构

**文件路径**: `magnet_cat_sync_{hash}.enc`（hash 为密码 SHA256 前12位）  
**存储位置**: Gitee 私有仓库  
**加密方式**: AES-256-GCM（PBKDF2 派生密钥）

解密后的 JSON 数据结构：

| 字段 | 类型 | 必填 | 描述 |
|------|------|------|------|
| `version` | Number | 是 | 数据格式版本号，当前为 1 |
| `lastSyncTime` | String | 是 | 同步时间（ISO 8601 格式） |
| `items` | Array | 是 | 收藏数据数组，元素结构同 6.1.1 |

#### 6.2.2 加密信封结构

| 字段 | 类型 | 描述 |
|------|------|------|
| `s` | String | Salt（Base64 编码，16字节） |
| `i` | String | IV（Base64 编码，12字节） |
| `a` | String | AuthTag（Base64 编码，16字节） |
| `d` | String | 加密数据（Base64 编码） |

#### 6.2.3 机器密钥加密信封结构

| 字段 | 类型 | 描述 |
|------|------|------|
| `i` | String | IV（Base64 编码，12字节） |
| `a` | String | AuthTag（Base64 编码，16字节） |
| `d` | String | 加密数据（Base64 编码） |

### 6.3 IPC 通信数据

#### 6.3.1 搜索相关 IPC 事件

| 事件名 | 方向 | 数据类型 | 描述 |
|--------|------|---------|------|
| `search` | 渲染→主 | String | 搜索关键词 |
| `search-total` | 主→渲染 | Number | 搜索结果总数 |
| `search-result` | 主→渲染 | Object | 单条搜索结果（含 title/size/sizeBytes/magnet/magnetCount） |
| `search-complete` | 主→渲染 | Object | 搜索完成统计（success/fail/duration） |
| `search-error` | 主→渲染 | String | 搜索错误信息 |

#### 6.3.2 窗口控制 IPC 事件

| 事件名 | 方向 | 数据类型 | 描述 |
|--------|------|---------|------|
| `minimize` | 渲染→主 | - | 最小化窗口 |
| `maximize` | 渲染→主 | - | 最大化/还原窗口 |
| `close` | 渲染→主 | - | 关闭窗口 |
| `isMaximized` | 渲染↔主 | Boolean | 查询窗口是否最大化 |

#### 6.3.3 云同步 IPC 事件

| 事件名 | 方向 | 数据类型 | 描述 |
|--------|------|---------|------|
| `cloud-load-settings` | 渲染↔主 | Object | 加载云同步配置 |
| `cloud-auto-unlock` | 渲染↔主 | Object | 自动解锁配置 |
| `cloud-unlock` | 渲染↔主 | Object | 密码解锁 |
| `cloud-save-settings` | 渲染↔主 | Object | 保存云同步配置 |
| `cloud-test-connection` | 渲染↔主 | Object | 测试 Gitee 连接 |
| `cloud-sync` | 渲染↔主 | Object | 执行同步（push/pull） |
| `cloud-get-status` | 渲染↔主 | Object | 获取同步状态 |
| `cloud-is-configured` | 渲染↔主 | Boolean | 查询是否已配置 |
| `cloud-favorite-changed` | 渲染↔主 | Object | 通知收藏变更（触发延迟同步） |
| `cloud-auto-sync` | 渲染↔主 | Object | 自动拉取同步 |
| `cloud-reset-password` | 渲染↔主 | Object | 重置密码 |
| `cloud-reset-password-push` | 渲染↔主 | Object | 重置密码并推送本地数据 |
| `cloud-full-sync` | 渲染↔主 | Object | 完整同步流程 |
| `cloud-clear-all-data` | 渲染↔主 | Object | 清除所有持久数据 |

---

## 7. 业务流程图

### 7.1 搜索流程

```
┌─────────┐     ┌──────────┐     ┌──────────────┐
│ 用户输入  │────→│ 点击搜索/  │────→│ 关键词Base64 │
│ 关键词   │     │ 按回车    │     │    编码      │
└─────────┘     └──────────┘     └──────┬───────┘
                                         │
                                         ▼
                                ┌────────────────┐
                                │ 请求搜索页面     │
                                │ (GET /search)   │
                                └───────┬────────┘
                                        │
                              ┌─────────┴─────────┐
                              │                   │
                              ▼                   ▼
                        ┌──────────┐        ┌──────────┐
                        │ 请求成功  │        │ 请求失败  │
                        └─────┬────┘        └─────┬────┘
                              │                   │
                              ▼                   ▼
                    ┌─────────────────┐    ┌──────────────┐
                    │ 正则提取链接列表  │    │ 发送搜索错误  │
                    │ 和文件大小列表   │    │  事件到渲染   │
                    └────────┬────────┘    └──────────────┘
                             │
                             ▼
                  ┌─────────────────────┐
                  │ 遍历链接列表         │
                  │ 逐条请求详情页       │
                  │ (间隔100ms)         │
                  └──────────┬──────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
                    ▼                 ▼
              ┌───────────┐    ┌───────────┐
              │ 提取磁力链接│    │ 未找到磁力 │
              │ 计算磁力数  │    │  跳过     │
              └─────┬─────┘    └───────────┘
                    │
                    ▼
          ┌──────────────────┐
          │ 发送 search-result │
          │ 事件到渲染进程     │
          │ (含排序插入DOM)    │
          └────────┬─────────┘
                   │
                   ▼
          ┌──────────────────┐
          │ 所有结果处理完毕   │
          │ 发送search-complete│
          │ (含成功/失败统计)  │
          └──────────────────┘
```

### 7.2 收藏流程

```
┌──────────┐     ┌────────────┐     ┌─────────────────┐
│ 用户点击   │────→│ 检查是否已  │────→│ 已收藏：从列表   │
│ 收藏按钮   │     │  收藏      │     │ 中移除该链接     │
└──────────┘     └─────┬──────┘     └────────┬────────┘
                       │                     │
                       │ 未收藏               │
                       ▼                     │
               ┌──────────────┐              │
               │ 添加到收藏列表 │              │
               │ (unshift)    │              │
               └──────┬───────┘              │
                      │                      │
                      ▼                      ▼
               ┌──────────────────────────────────┐
               │ 写入 localStorage                  │
               │ (magnet_favorites)                │
               └───────────────┬──────────────────┘
                               │
                               ▼
               ┌──────────────────────────────────┐
               │ 更新 UI 状态                       │
               │ · 收藏按钮样式切换                  │
               │ · 工具栏收藏计数更新                 │
               └───────────────┬──────────────────┘
                               │
                               ▼
               ┌──────────────────────────────────┐
               │ 检查云同步是否已配置                 │
               │ · 已配置：触发延迟云同步             │
               │ · 未配置：跳过                      │
               └──────────────────────────────────┘
```

### 7.3 云同步流程

```
┌──────────────────────────────────────────────────────────┐
│                      云同步配置流程                        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────┐    ┌──────────┐    ┌───────────────┐        │
│  │ 打开设置 │───→│ 检查锁定 │───→│ 已锁定：输入  │        │
│  │   页面   │    │   状态   │    │ 密码解锁     │        │
│  └─────────┘    └────┬─────┘    └──────┬────────┘        │
│                      │                 │                  │
│                      │ 未锁定           │ 解锁成功         │
│                      ▼                 ▼                  │
│              ┌────────────────────────────────┐           │
│              │     配置 Gitee 信息             │           │
│              │  · 输入私人令牌                 │           │
│              │  · 输入仓库名称                 │           │
│              │  · 设置加密密码（≥6位）          │           │
│              └──────────────┬─────────────────┘           │
│                             │                             │
│                             ▼                             │
│              ┌────────────────────────────────┐           │
│              │   测试连接（可选）               │           │
│              │  · 验证令牌有效性               │           │
│              │  · 验证仓库存在性               │           │
│              └──────────────┬─────────────────┘           │
│                             │                             │
│                             ▼                             │
│              ┌────────────────────────────────┐           │
│              │   保存设置                      │           │
│              │  · 密码哈希存储                 │           │
│              │  · 令牌加密存储                 │           │
│              │  · 密码机器密钥加密存储          │           │
│              └──────────────┬─────────────────┘           │
│                             │                             │
│                             ▼                             │
│              ┌────────────────────────────────┐           │
│              │   首次保存：推送本地数据到云端    │           │
│              │   非首次：拉取云端数据覆盖本地    │           │
│              └────────────────────────────────┘           │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

```
┌──────────────────────────────────────────────────────────┐
│                      自动同步流程                         │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────┐     ┌──────────────────────────┐   │
│  │ 应用启动          │────→│ 3秒后自动拉取云端数据     │   │
│  │                  │     │ 覆盖本地 localStorage    │   │
│  └──────────────────┘     └──────────────────────────┘   │
│                                                          │
│  ┌──────────────────┐     ┌──────────────────────────┐   │
│  │ 收藏/取消收藏      │────→│ 延迟3秒后自动推送        │   │
│  │                  │     │ 本地数据到云端            │   │
│  └──────────────────┘     └──────────────────────────┘   │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 7.4 推送同步详细流程

```
┌────────────────┐
│ 读取本地收藏数据 │
└───────┬────────┘
        │
        ▼
┌────────────────────┐
│ 构建同步数据结构     │
│ {version, items,   │
│  lastSyncTime}     │
└───────┬────────────┘
        │
        ▼
┌────────────────────┐
│ JSON序列化 +        │
│ AES-256-GCM加密    │
└───────┬────────────┘
        │
        ▼
┌────────────────────┐
│ 计算同步文件路径     │
│ (密码SHA256前12位)  │
└───────┬────────────┘
        │
        ▼
┌────────────────────┐     ┌──────────────────┐
│ 检查云端是否已有文件 │────→│ 已有：PUT更新文件 │
└───────┬────────────┘     └──────────────────┘
        │
        │ 不存在
        ▼
┌────────────────────┐
│ POST创建新文件      │
└───────┬────────────┘
        │
        ▼
┌────────────────────┐
│ 更新本地lastSyncTime│
└────────────────────┘
```

---

## 8. 项目风险与应对策略

### 8.1 技术风险

| 风险编号 | 风险描述 | 影响程度 | 可能性 | 应对策略 |
|---------|---------|---------|--------|---------|
| R-T01 | 搜索源站点不可用或变更页面结构 | 高 | 高 | 1. 增加多搜索源支持；2. 搜索失败时提供友好提示；3. 抽象搜索源接口，便于快速适配新源 |
| R-T02 | Gitee API 限流导致同步失败 | 中 | 中 | 1. 实现请求限流控制；2. 同步失败时自动重试（最多3次）；3. 提供手动同步入口 |
| R-T03 | Electron 安全漏洞（contextIsolation: false） | 高 | 中 | 1. 短期：确保不加载不可信远程内容；2. 中期：迁移至 preload 脚本 + contextIsolation: true 架构；3. 长期：启用完整的 Electron 安全最佳实践 |
| R-T04 | Vue.js CDN 加载失败导致应用白屏 | 高 | 低 | 1. 将 Vue 和 Vue Router 打包为本地依赖；2. 添加离线降级处理 |
| R-T05 | localStorage 容量限制（约5MB） | 中 | 中 | 1. 监控存储使用量；2. 超出限制时提示用户清理；3. 考虑迁移至 IndexedDB |
| R-T06 | 机器特征变更导致自动解锁失败 | 中 | 低 | 1. 提供手动密码解锁作为降级方案；2. 机器特征变更时提示用户重新输入密码 |

### 8.2 业务风险

| 风险编号 | 风险描述 | 影响程度 | 可能性 | 应对策略 |
|---------|---------|---------|--------|---------|
| R-B01 | 磁力搜索源法律合规风险 | 高 | 中 | 1. 应用仅提供搜索工具，不存储任何资源内容；2. 搜索源可配置化，用户自行选择搜索源；3. 添加免责声明 |
| R-B02 | 用户忘记加密密码导致云端数据不可恢复 | 高 | 中 | 1. 密码重置功能（使用本地数据覆盖云端）；2. 设置页明确提示密码重要性；3. 密码强度指示器引导用户设置强密码 |
| R-B03 | Gitee 服务不稳定或政策变更 | 中 | 低 | 1. 抽象云存储接口，便于迁移至其他平台；2. 本地数据优先，云同步为增强功能；3. 支持导出/导入功能作为数据备份方案 |

### 8.3 用户体验风险

| 风险编号 | 风险描述 | 影响程度 | 可能性 | 应对策略 |
|---------|---------|---------|--------|---------|
| R-U01 | 搜索等待时间过长导致用户流失 | 高 | 中 | 1. 搜索结果逐条流入，提供即时反馈；2. 显示进度条和已获取数量；3. 优化搜索请求并发策略 |
| R-U02 | 云同步配置复杂度高 | 中 | 高 | 1. 提供详细的配置引导说明；2. 自动解锁减少重复输入；3. 连接测试即时反馈 |
| R-U03 | 深色/浅色主题切换闪烁 | 低 | 低 | 1. 页面加载前应用已保存的主题（内联脚本）；2. CSS 过渡动画平滑切换 |

### 8.4 风险优先级矩阵

```
                    影响程度
              低         中         高
         ┌──────────┬──────────┬──────────┐
    高   │ R-U02    │ R-T02    │ R-T01    │
可       │          │ R-B03    │ R-T03    │
能       │          │          │ R-B01    │
性       ├──────────┼──────────┼──────────┤
    中   │          │ R-T05    │ R-B02    │
         │          │ R-T06    │ R-U01    │
         ├──────────┼──────────┼──────────┤
    低   │ R-U03    │          │ R-T04    │
         │          │          │          │
         └──────────┴──────────┴──────────┘
```

---

## 9. 附录

### 9.1 技术架构概览

```
┌─────────────────────────────────────────────────────────┐
│                    Electron 应用                         │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │               主进程 (main.js)                    │   │
│  │                                                   │   │
│  │  · 窗口管理（主窗口 + 调试窗口）                    │   │
│  │  · IPC 通信处理                                    │   │
│  │  · 搜索逻辑（HTTP 请求 + HTML 解析）              │   │
│  │  · 云同步编排                                      │   │
│  │  · 日志系统                                        │   │
│  └────────────────────┬────────────────────────────┘   │
│                       │ require                         │
│  ┌────────────────────┴────────────────────────────┐   │
│  │            云同步模块 (cloudSync.js)              │   │
│  │                                                   │   │
│  │  · AES-256-GCM 加密/解密                         │   │
│  │  · PBKDF2 密钥派生                               │   │
│  │  · 机器密钥加密                                   │   │
│  │  · Gitee API 交互                                │   │
│  │  · 本地配置管理                                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │             渲染进程 (index.html)                  │   │
│  │                                                   │   │
│  │  ┌─────────────────────────────────────────┐     │   │
│  │  │          Vue 3 + Vue Router 4             │     │   │
│  │  │                                           │     │   │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ │     │   │
│  │  │  │SearchPage│ │Favorites │ │ Settings │ │     │   │
│  │  │  │   .js    │ │  Page.js │ │  Page.js │ │     │   │
│  │  │  └──────────┘ └──────────┘ └──────────┘ │     │   │
│  │  └─────────────────────────────────────────┘     │   │
│  │                                                   │   │
│  │  · localStorage（收藏数据 + 主题设置）            │   │
│  │  · Clipboard API（磁力链接复制）                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 9.2 外部依赖

| 依赖 | 版本 | 用途 | 类型 |
|------|------|------|------|
| Electron | ^28.3.3 | 桌面应用框架 | devDependency |
| electron-builder | ^26.8.1 | 应用打包 | devDependency |
| electron-icon-builder | ^2.0.1 | 图标生成 | devDependency |
| electron-packager | ^17.1.2 | 应用打包（备选） | devDependency |
| axios | ^1.6.0 | HTTP 请求 | dependency |
| base-64 | ^1.0.0 | Base64 编解码 | dependency |
| Vue 3 | CDN | 前端框架 | runtime |
| Vue Router 4 | CDN | 前端路由 | runtime |

### 9.3 关键算法说明

#### 9.3.1 磁力数生成算法

磁力数（magnetCount）是一个基于磁力链接哈希的伪随机数值，用于模拟资源的"热度"指标，范围 1-50。

```
输入: magnetHash (String)
输出: magnetCount (Number, 1-50)

算法:
1. 初始化 hash = 0
2. 遍历 magnetHash 的每个字符:
   hash = ((hash << 5) - hash) + charCode
   hash = hash & hash  (转为32位整数)
3. magnetCount = Math.abs(hash % 50) + 1
```

#### 9.3.2 搜索关键词编码

搜索关键词使用 Base64 编码后作为 URL 参数传递给搜索源。

```
输入: keyword (String, UTF-8)
输出: encodedKeyword (String)

算法:
1. Buffer.from(keyword, 'utf-8').toString('base64')
2. 移除所有 '=' 填充字符
```

#### 9.3.3 同步文件路径生成

基于用户密码的 SHA256 哈希前12位生成唯一的同步文件名，确保不同密码对应不同的云端文件。

```
输入: password (String)
输出: filePath (String, 格式: magnet_cat_sync_{hash}.enc)

算法:
1. hash = SHA256("magnet-cat-file-derive-" + password)
2. shortHash = hash.substring(0, 12)
3. filePath = "magnet_cat_sync_" + shortHash + ".enc"
```

### 9.4 构建与发布

| 项目 | 说明 |
|------|------|
| 构建工具 | electron-builder + PowerShell 脚本 |
| Windows 目标 | dir (x64) |
| macOS 目标 | dmg (x64, arm64) |
| 资源打包 | asar 归档 |
| 语言包精简 | 仅保留 zh-CN, en-US, zh-TW |
| 输出目录 | `release/` |
| 构建命令 | `npm run pack`（完整构建）或 `npm run build:win` / `npm run build:mac` |

### 9.5 术语表

| 术语 | 定义 |
|------|------|
| 磁力链接 | 一种基于 DHT 网络的资源定位标识，格式为 `magnet:?xt=urn:btih:...` |
| 磁力数 | 基于磁力链接哈希计算的伪随机数值，用于模拟资源热度排序 |
| Gitee | 国内的 Git 代码托管平台，本项目中用作云同步的存储后端 |
| 私人令牌 | Gitee 提供的 API 访问凭证，用于程序化访问 Gitee API |
| PBKDF2 | Password-Based Key Derivation Function 2，基于密码的密钥派生函数 |
| AES-256-GCM | Advanced Encryption Standard 256位密钥 Galois/Counter Mode，认证加密算法 |
| AuthTag | GCM 模式生成的认证标签，用于验证数据完整性和真实性 |
| 机器密钥 | 基于主机名、用户名、平台、架构等机器特征派生的加密密钥 |
| asar | Electron 的应用资源归档格式，将应用文件打包为单个只读文件 |
| IPC | Inter-Process Communication，Electron 主进程与渲染进程间的通信机制 |

---

> **文档维护说明**: 本文档应随产品迭代持续更新，每次功能变更后需同步修改对应章节，并更新文档版本号。
